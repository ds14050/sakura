image: Visual Studio 2017

configuration:
  - Release
  - Debug

platform:
  - Win32
  - x64
  - MinGW

build_script:
- cmd: >
    for %%B in (
    checkEncoding.bat
    build-by-cmake.bat
    sakura\postBuild.bat
    build-chm.bat
    build-installer.bat
    externals\cppcheck\install-cppcheck.bat
    run-cppcheck.bat
    zipArtifacts.bat
    ) do (cmd /C "pushd "%%~dpB" & %%~nxB %PLATFORM% %CONFIGURATION% all")
    || (echo error %%B & exit /b 0)

after_build:
- cmd: set SAKURA_TEST=%PLATFORM%\%CONFIGURATION%\sakura_test.exe

test:
  assemblies:
    only:
      - '%SAKURA_TEST%'

test_script:
- cmd: |
    "%SAKURA_TEST%" --gtest_list_tests
    "%SAKURA_TEST%" | tests\test_result_filter_tell_AppVeyor.bat

artifacts:
- path: 'sakura-*$(platform)-$(configuration)*.zip'
